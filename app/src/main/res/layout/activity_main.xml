<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".MainActivity">

    <!-- Game Surface View - Zero-copy path to SurfaceFlinger/HWC -->
    <com.closenow.render.GameSurfaceView
        android:id="@+id/gameSurfaceView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:keepScreenOn="true" />

    <!-- Minimal UI Overlays (on top of SurfaceView) -->
    <FrameLayout
        android:id="@+id/overlayContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Stats/Connection HUD - Top Left -->
        <FrameLayout
            android:id="@+id/statsOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_margin="16dp"
            android:background="@color/overlay_background"
            android:padding="12dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/statsTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/stats_text"
                android:textSize="12sp"
                android:fontFamily="monospace"
                android:text="@string/initializing" />

        </FrameLayout>

        <!-- Connection Status - Top Right -->
        <FrameLayout
            android:id="@+id/connectionOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="16dp"
            android:background="@color/overlay_background"
            android:padding="12dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/connectionTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="12sp"
                android:text="@string/connecting" />

        </FrameLayout>

        <!-- Thermal Warning - Center Bottom -->
        <FrameLayout
            android:id="@+id/thermalOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="48dp"
            android:background="@color/error_red"
            android:padding="16dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/thermalTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:text="@string/thermal_warning_message" />

        </FrameLayout>

    </FrameLayout>

</FrameLayout>